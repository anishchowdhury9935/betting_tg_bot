const config = require("../../../config");
const { TryCatch } = require("../../helper/helperMain");
const bot_commands = require('../commands/bot_commands');

const index = {

}

module.exports = index;